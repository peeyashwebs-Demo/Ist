# Copy to .env.local (gitignored) and adjust as needed.

# Points the dashboard's API client (lib/api/client.ts) at the real backend.
# Defaults to http://localhost:3000 if unset. The live Railway deployment:
NEXT_PUBLIC_API_BASE_URL=https://kudimata-securities-backend-production.up.railway.app
